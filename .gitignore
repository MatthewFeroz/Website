# Dependencies
node_modules/

# Convex / env (all dotenv variants — keep secrets like sk_live out of git)
.env
.env.*
!.env.example

# OS
.DS_Store

# IDE
.idea/
*.swp
*.swo

# Cloudflare Wrangler
.wrangler/

# Internal setup notes / one-off scripts (operational identifiers — keep out of the
# published site & repo; Jekyll `exclude` proved unreliable here)
SETUP-AUTH.md
clerk-dns-cloudflare.sh
